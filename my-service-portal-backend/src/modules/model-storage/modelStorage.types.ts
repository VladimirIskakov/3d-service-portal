export interface StoredModelFile {
  fileName: string;
  publicPath: string;
  sizeBytes: number;
}

export interface StoredImageFile {
  fileName: string;
  publicPath: string;
  sizeBytes: number;
}
